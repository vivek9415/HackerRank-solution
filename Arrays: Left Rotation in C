#include <assert.h>
#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
main()
{
    int a[100000],i,n,d;
    scanf("%d",&n);
    scanf("%d",&d);
    if((1<=d&&d<=n))
    {
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=d;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    for(int i=0;i<d;i++)
    {
        printf("%d ",a[i]);
    }
}
return 0;

}
